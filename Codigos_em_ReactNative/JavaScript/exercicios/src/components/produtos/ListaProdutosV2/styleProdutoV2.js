import { StyleSheet } from 'react-native'

export const stylesProdutoV2 = StyleSheet.create({
    lista: {
        marginTop: 20,
        width:'50%',
        borderColor:'black',
        borderWidth:2,
        borderRadius:10,
        padding:10,
    }
});