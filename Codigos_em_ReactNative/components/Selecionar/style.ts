import { StyleSheet } from "react-native";

export const styleSelecionar = StyleSheet.create ({
    circulo:{
        width:20,
        height:20,
        borderRadius:10,
        borderWidth:1
    }
});