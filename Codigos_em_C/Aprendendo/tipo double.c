#include <stdio.h>

int main(void) {
  float pagamento = 1570.90;
  double salario = 1570.90;
  printf("Pagamento = %f\n", pagamento);
  printf("Salário = %f\n", salario);
  //Pq uma parte do pagamento em decimal é 24?
  return 0;
}